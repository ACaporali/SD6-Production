{% extends "SD6ProductionAppBundle:Include:layout.html.twig" %}

{% block title %}Actualites - {{ parent() }}{% endblock %}

{% block head %}
  {{ parent() }}
  <meta name="description" content="">
  <meta name="keywords" content="">
{% endblock %}

{% block site_body %}
	<div class="bordures">
	  	{{ parent() }}
	  	<h1>Actualités</h1>
		<div class="{{Annonce.Categories[0].nom}} {{Annonce.slug}}">  		
		  	<div class="row">
			  	<div class="image">
					{% if Annonce.Image is not null %}
						<img src="{{Annonce.Image.Url}}" alt="{{Annonce.Image.Alt}}">
					{% else %}
						<p>Pas de visuel</p>
					{% endif %}
				</div>
				<div class="contenu">
					<div class="titre">
						<h2>{{Annonce.Titre}}</h2>
					</div>
					<div class="texte">
						<p>{{Annonce.contenu}}</p>
					</div>
				</div>
			</div>
		</div>
	</div>

{% endblock %}