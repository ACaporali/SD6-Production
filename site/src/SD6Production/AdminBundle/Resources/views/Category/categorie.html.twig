{% extends "SD6ProductionAppBundle:Include:layout.html.twig" %}

{% block title %}Categories - {{ parent() }}{% endblock %}

{% block head %}
  {{ parent() }}
  <meta name="description" content="">
  <meta name="keywords" content="">
{% endblock %}

{% block site_body %}
	<div class="admin-categories">
  	{{ parent() }}
  	<h1 class="beige text-center">Categories</h1>
    <hr class="hr-beige">
		<div class="bordures">
	  	<div class="row">
		  	<a href="{{ path('sd6_production_admin_add_category') }}" class="btn btn-primary">Ajouter une categorie</a>
        <div class="liste">
          <table class="table table-hover">
            <thead>
              <tr>
                <th>Nom</th>
                <th>Actions</th>
              </tr>
            </thead>
            <tbody>
              {% for category in listeCategories %}
                  <tr>
                    <td>{{category.name}}</td>
                    <td>
                      <div class="boutons-admin">
                        <div class="btn-group">
                          <a href="{{path('sd6_production_admin_edite_category', {'idCategory': category.id})}}" class="btn btn-default">Modifier</a>
                          <a href="#" class="supprimer btn btn-danger" data-id="{{category.id}}">Supprimer</a>
                        </div>
                      </div>
                    </td>
                  </tr>
              {% endfor %}
            </tbody>
          </table>
        </div>
		  </div>
	  </div>
	</div>

{% endblock %}
